import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

const ChatSlice = createSlice({
    name: 'chat',
    initialState,
    reducers: {
    }
});

export default ChatSlice.reducer;